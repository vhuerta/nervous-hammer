/*eslint-disable no-unused-vars*/
import React from 'react';
import createReactClass from 'create-react-class';

const Recover = createReactClass({
  render() {
    return (
      <div>
        <h1>Recover form</h1>
      </div>
    );
  },
});

export default Recover;
